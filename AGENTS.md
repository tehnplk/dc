## Rule
- Don't test build and deploy  if user not ask.

## Tool
- use `db-cli --help` to access database
- use `playwright-cli --help` to E2E test and when open url . You have to run `playwright-cli show` for give user to observ.

- use @HOSXP_API.md for research HIS database.

