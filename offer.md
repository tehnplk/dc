# ข้อเสนองานถัดไป

ประเมิน ณ 11 ก.ย. 2569 หลังทำระบบล็อกอิน SSO, บทบาทผู้ใช้ และการป้องกันการเรียกจากเว็บอื่นเสร็จ
เรียงตามความเสี่ยงจริง ไม่ใช่ตามความยากง่าย

| ลำดับ | เรื่อง | ดำเนินการแล้ว |
|---|---|---|
| 0 | ไม่ย้ายไป Auth.js — อยู่กับที่เขียนเองด้วย jose | yes (สรุปแล้ว ไม่มีงานต้องทำ) |
| 1 | ปิดข้อมูลผู้ป่วยบนแผนที่สาธารณะ | no |
| 2 | rate limit หน้า `/auth/admin` | yes (11 ก.ย. 2569) |
| 3 | เตะผู้ใช้ออกกลางคันได้ (token_version) | no |
| 4 | บันทึกการเปิดดูข้อมูลผู้ป่วย (case_view_log) | no |
| 5 | เตรียมขึ้น production | no |

ทำข้อไหนเสร็จให้แก้ช่อง "ดำเนินการแล้ว" เป็น yes

## 0. เรื่อง Auth.js — ข้อเสนอคือ "ไม่ต้องย้าย"

ระบบล็อกอินตอนนี้เขียนเองด้วย `jose` ล้วน (OAuth2 authorization code + PKCE, ตรวจ `id_token` กับ JWKS,
แล้วออกคุกกี้เซสชันของแอปเอง) รวมโค้ดทั้งหมดราว 200 บรรทัด

| | ที่ทำอยู่ | ถ้าย้ายไป Auth.js |
|---|---|---|
| dependency | `jose` ที่มีอยู่แล้ว | next-auth + adapter |
| โค้ด auth | ~200 บรรทัด อ่านจบใน 10 นาที | config + callbacks ที่ต้องรู้ว่า hook ไหนยิงตอนไหน |
| schema | `users` ที่ผูกกับ `c_org` / `user_role` โดยตรง | ต้องมี Account / Session / VerificationToken ของ adapter |
| flow เลือกหน่วยงานก่อนสร้างบัญชี | เขียนตรง ๆ ใน callback | ต้องดัน `signIn` callback + redirect เอง |

มี provider เดียว ไม่มี refresh token ไม่มี social login ไม่มี magic link
ย้ายแล้วได้ abstraction เพิ่มแต่โค้ดไม่ได้ลด — **แนะนำให้อยู่กับของเดิม**

---

## 1. ปิดข้อมูลผู้ป่วยบนแผนที่สาธารณะ 🔴 เร่งด่วนที่สุด

**ปัญหา** `/dashboard` เปิดสาธารณะตามที่สั่งไว้ แต่แท็บ GIS popup ของหมุดขึ้น
**ชื่อผู้ป่วย + เลขทะเบียนเคส + ตำบล/อำเภอ + วันเริ่มป่วย** พร้อม**พิกัดบ้าน**
ใครเปิดเว็บก็เห็น ไม่ต้องล็อกอิน (ดู `src/components/CaseMap.tsx` บรรทัด ~49 และ `src/app/(app)/dashboard/page.tsx` ที่ไม่เรียก `currentUser()`)

**เสนอ** ยังไม่ล็อกอิน → ซ่อนชื่อกับเลขเคส และเบลอพิกัดเป็นระดับหมู่บ้าน/ตำบล
หรือถ้าจะเอาง่ายและปลอดภัยกว่า ให้แท็บ GIS ต้องล็อกอิน (แท็บ Dashboard ที่เป็นตัวเลขรวมยังสาธารณะได้)

**แรง** ~30 นาที

## 2. rate limit หน้า `/auth/admin` ✅ ทำแล้ว

**ปัญหา** เดารหัสบัญชีสำรองได้ไม่จำกัดครั้ง ไม่มีหน่วงเวลา ไม่มี lockout
เป็นบัญชีที่มีสิทธิ์จัดการระบบเต็ม และรหัสมาจาก `.env` ตัวเดียว

**ที่ทำ** `src/lib/ratelimit.ts` นับครั้งที่ผิดต่อ IP ในหน่วยความจำ
ผิดได้ฟรี 3 ครั้ง (คนพิมพ์ผิดจริง) จากนั้นหน่วง 5 → 15 → 60 → 300 วินาที (ค้างที่ 300)
ระหว่างโดนหน่วงจะไม่ไปแตะรหัสผ่านเลย และล็อกอินผ่านเมื่อไหร่ล้างประวัติทันที
เทสที่ `src/lib/ratelimit.test.mts` (`npm test`)

ยังไม่ได้ทำ: ตัวนับอยู่ในหน่วยความจำของ process — ขึ้นหลายอินสแตนซ์เมื่อไหร่ต้องย้ายไป DB/redis
และยังไม่มีเพดานรวมทั้งระบบ (กระจายไอพีมายิงพร้อมกันยังผ่านด่านนี้ได้)

## 3. เตะผู้ใช้ออกกลางคันได้ 🟡

**ปัญหา** คุกกี้เซสชันเป็น JWT ไร้ state ตอนนี้ปิดบัญชี (`is_active = false`) ถึงจะเตะออกได้
แต่ "ออกจากระบบทุกอุปกรณ์" หรือเพิกถอนเซสชันเดียวยังทำไม่ได้ (เช่นเครื่องหาย)

**เสนอ** คอลัมน์ `token_version` ใน `users` ใส่ค่าลง JWT แล้วเทียบตอน `currentUser()`
กดออกทุกอุปกรณ์ = `token_version + 1`

**แรง** ~30 นาที

## 4. บันทึกการเปิดดูข้อมูลผู้ป่วย 🟡

**ปัญหา** ตาราง `case_view_log` มีในสคีมาตั้งแต่แรก แต่**ยังไม่มีโค้ดเขียนลงเลยสักบรรทัด**
ข้อมูลระบาดวิทยาเป็นข้อมูลสุขภาพส่วนบุคคล ควรตอบได้ว่าใครเปิดดูเคสไหนเมื่อไหร่

**เสนอ** เขียน log ตอนเปิด modal กิจกรรม และตอนเปิดไฟล์แนบ (รูป/PDF)

**แรง** ~1 ชม.

## 5. เตรียมขึ้น production 🟢

- `NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` — ต้องตั้งค่าเดียวกันทุกอินสแตนซ์ถ้ารันมากกว่า 1 ตัว
  ไม่งั้น action ที่เข้ารหัส closure ไว้จะใช้ข้ามอินสแตนซ์ไม่ได้
- คุกกี้ตั้ง `secure` เมื่อ `NODE_ENV=production` แล้ว — ต้องมี HTTPS จริง ไม่งั้นล็อกอินไม่ติด
- `APP_URL` ต้องตรงกับ redirect URI ที่ลงทะเบียนไว้กับ PLKHealth SSO
- ถ้าขึ้นหลัง reverse proxy / CDN คนละโดเมน ต้องเพิ่ม `serverActions.allowedOrigins` ใน `next.config.ts`
- กติกาใน `src/proxy.ts` บล็อก `Sec-Fetch-Site: same-site` ด้วย (subdomain อื่นของโดเมนเดียวกัน)
  ถ้าวันหลังมีระบบพี่น้องที่ต้องเรียกข้าม subdomain ต้องมาเปิดทางตรงนี้

---

## เรื่องที่รู้อยู่แล้วว่ายังค้าง (ยังไม่เสนอให้ทำตอนนี้)

- ไฟล์ที่ลบกิจกรรมทิ้งแล้วยังค้างอยู่ใน `public/uploads` — ยังไม่มีสคริปต์กวาดไฟล์กำพร้า
  (แถวใน DB หายตาม FK cascade แล้ว) รอจนพื้นที่เริ่มเป็นปัญหาค่อยทำ
- แก้ไฟล์แนบใน modal แก้ไขกิจกรรมไม่ได้ ต้องลบกิจกรรมแล้วบันทึกใหม่
- `cid` ของผู้ใช้เก็บเป็นตัวเลขตรง ๆ (ใช้ส่งแจ้งเตือนไลน์หมอพร้อม จึง hash ไม่ได้)
  ถ้าต้องการเข้ารหัสที่ระดับคอลัมน์ต้องคุยเรื่องการจัดการกุญแจก่อน

---

ถ้าไม่ระบุเป็นอย่างอื่น จะเริ่มจากข้อ 1 แล้วต่อข้อ 2
